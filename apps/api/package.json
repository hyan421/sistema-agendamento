{
  "name": "@navalha/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --eval \"console.error('API ainda nao foi implementada (lote 2+).'); process.exit(1)\"",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "db:migrate": "node --eval \"console.error('Migrations ainda nao foram implementadas (lote 2).'); process.exit(1)\"",
    "db:seed": "node --eval \"console.error('Seed ainda nao foi implementado (lote 2).'); process.exit(1)\""
  },
  "devDependencies": {
    "@types/node": "24.5.2"
  }
}
